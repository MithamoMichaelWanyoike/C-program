#include <stdio.h>

int main() {
    // Define the 1D array
    int marks[3] = {67, 78, 97};
    
    // Print the array elements
    for(int i = 0; i < 3; i++) {
        printf("%d ", marks[i]);
    }
    
    return 0;
}